The script `rl_adaptive_binning.py` is used to train adaptive binning.

The script `02_build_view.py` is used to build the view.

The script `03_train_gam_mvatt.py` calculates view relevance using training to derive the GRN.
